[build]
  publish = "dist"
  command = "npm run build"

# Handle SPA routing - redirect all paths to index.html so client-side routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
